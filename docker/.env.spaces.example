# Copy to .env.spaces (gitignored) on the droplet, fill in real values.
# Consumed by sync-backup-offsite.sh (source it before running, or load
# via the crontab entry) — see that script's own header comment.
DO_SPACES_BUCKET=
DO_SPACES_REGION=syd1
DO_SPACES_ACCESS_KEY=
DO_SPACES_SECRET_KEY=
