<div class="row mb-2">
  <div class="col-12">
    <h1>Edit <?= $this->escaper->escapeHtml($requirement->display_id) ?></h1>
  </div>
</div>

<div class="row">
  <div class="col-12 col-md-6">
    <div class="card">
      <div class="card-body">
        <form method="post" action="<?= $this->url->get('requirements/requirements/update/' . $requirement->id) ?>">
          <div class="form-group">
            <label for="title">Title</label>
            <input type="text" class="form-control" id="title" name="title" value="<?= $this->escaper->escapeHtml((string) $requirement->title) ?>" required>
          </div>
          <div class="form-group">
            <label for="description">Description</label>
            <textarea class="form-control" id="description" name="description" rows="5"><?= $this->escaper->escapeHtml((string) $requirement->description) ?></textarea>
          </div>
          <div class="form-group">
            <label for="origin_session">Originated <span class="text-muted">(e.g. "Session 12")</span></label>
            <input type="text" class="form-control" id="origin_session" name="origin_session" value="<?= $this->escaper->escapeHtml((string) $requirement->origin_session) ?>">
          </div>
          <div class="form-group">
            <label for="status">Status</label>
            <select class="form-control" id="status" name="status">
<?php foreach ($statuses as $value => $label): ?>
              <option value="<?= $value ?>" <?= $requirement->status === $value ? 'selected' : '' ?>><?= $label ?></option>
<?php endforeach; ?>
            </select>
          </div>
<?php if ($requirement->changelog_id): ?>
          <div class="form-group">
            <label class="d-block">Changelog decision</label>
            <p class="form-control-plaintext">Included in <a href="<?= $this->url->get('requirements/changelogs/view/' . $requirement->changelog_id) ?>"><?= $this->escaper->escapeHtml($requirement->Changelog->version) ?></a> — remove it from that changelog to change this.</p>
          </div>
<?php else: ?>
          <div class="form-group">
            <label class="d-block">Changelog decision <span class="text-muted">(only used when status is set to Complete)</span></label>
<?php foreach ($decisions as $value => $label): ?>
            <div class="form-check">
              <input class="form-check-input" type="radio" name="changelog_decision" id="changelog_decision_<?= $value ?>" value="<?= $value ?>" <?= $requirement->changelog_decision === $value ? 'checked' : '' ?>>
              <label class="form-check-label" for="changelog_decision_<?= $value ?>"><?= $label ?></label>
            </div>
<?php endforeach; ?>
          </div>
<?php endif; ?>
          <div class="form-group">
            <label for="changelog_note">Changelog note <span class="text-muted">(optional — release-facing wording, overrides the title when rendered)</span></label>
            <textarea class="form-control" id="changelog_note" name="changelog_note" rows="3"><?= $this->escaper->escapeHtml((string) $requirement->changelog_note) ?></textarea>
          </div>
          <button type="submit" class="btn btn-primary">Save changes</button>
          <a href="<?= $this->url->get('requirements/requirements/view/' . $requirement->id) ?>" class="btn btn-link">Cancel</a>
        </form>
      </div>
    </div>
  </div>
</div>
