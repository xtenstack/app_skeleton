<div class="row mb-2">
  <div class="col-12 d-flex justify-content-between align-items-center">
    <h1 class="mb-0"><?= $this->escaper->escapeHtml($ticket->title) ?></h1>
    <div>
      <a href="<?= $this->url->get('frontend/tickets/edit/' . $ticket->id) ?>" class="btn btn-outline-secondary">Edit</a>
      <a href="<?= $this->url->get('frontend/tickets') ?>" class="btn btn-link">Back to Support Requests</a>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-12 col-md-8">
    <div class="card mb-3">
      <div class="card-body">
        <dl class="row mb-0">
          <dt class="col-3">Status</dt>
          <dd class="col-9">
            <span class="badge badge-<?= $ticket->status === 'closed' ? 'success' : ($ticket->status === 'consolidated' ? 'secondary' : ($ticket->status === 'in_review' ? 'info' : 'primary')) ?>"><?= $this->escaper->escapeHtml($ticket->status) ?></span>
          </dd>
          <dt class="col-3">Project ID</dt>
          <dd class="col-9"><?= $ticket->project ? $this->escaper->escapeHtml($ticket->project) : '<span class="text-muted">Not set — required to qualify for SLA response timing</span>' ?></dd>
          <dt class="col-3">Type</dt>
          <dd class="col-9"><?= $this->escaper->escapeHtml($ticket->ticket_type) ?></dd>
          <dt class="col-3">Severity</dt>
          <dd class="col-9"><?= $this->escaper->escapeHtml($ticket->severity) ?></dd>
          <dt class="col-3">Description</dt>
          <dd class="col-9"><?= nl2br($this->escaper->escapeHtml((string) $ticket->description)) ?></dd>
          <dt class="col-3">Submitted</dt>
          <dd class="col-9"><?= $this->escaper->escapeHtml($ticket->created_at) ?></dd>
<?php if ($ticket->status === 'closed'): ?>
          <dt class="col-3">Closed</dt>
          <dd class="col-9"><?= $this->escaper->escapeHtml($ticket->closed_at) ?></dd>
<?php endif; ?>
        </dl>
      </div>
    </div>

    <div class="card mb-3">
      <div class="card-header">Attachments</div>
      <div class="card-body">
<?php if (count($ticket->Attachments)): ?>
        <ul class="list-unstyled mb-3">
<?php foreach ($ticket->Attachments as $attachment): ?>
          <li class="mb-1">
            <a href="<?= $this->url->get('frontend/tickets/download-attachment/' . $ticket->id . '/' . $attachment->id) ?>"><?= $this->escaper->escapeHtml($attachment->original_filename) ?></a>
            <span class="text-muted small">(<?= number_format($attachment->size_bytes / 1024, 1) ?> KB)</span>
          </li>
<?php endforeach; ?>
        </ul>
<?php else: ?>
        <p class="text-muted">No files attached.</p>
<?php endif; ?>
        <form method="post" action="<?= $this->url->get('frontend/tickets/upload-attachment/' . $ticket->id) ?>" enctype="multipart/form-data" class="form-inline">
          <input type="file" name="attachment" class="form-control-file mr-2" required>
          <button type="submit" class="btn btn-sm btn-secondary">Upload</button>
        </form>
      </div>
    </div>
  </div>
</div>
