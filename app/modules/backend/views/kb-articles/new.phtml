<div class="row mb-2">
  <div class="col-12">
    <h1>New article</h1>
  </div>
</div>

<div class="row">
  <div class="col-12 col-md-8">
    <div class="card">
      <div class="card-body">
        <form method="post" action="<?= $this->url->get('backend/kb-articles/create') ?>">
          <div class="form-group">
            <label for="title">Title</label>
            <input type="text" class="form-control" id="title" name="title" value="<?= $this->escaper->escapeHtml((string) $article->title) ?>" required>
          </div>
          <div class="form-group">
            <label for="enquiry_type_id">Enquiry type</label>
            <select class="form-control" id="enquiry_type_id" name="enquiry_type_id" required>
              <option value="">— choose —</option>
<?php foreach ($enquiryTypes as $enquiryType): ?>
              <option value="<?= $enquiryType->id ?>" <?= (int) $article->enquiry_type_id === (int) $enquiryType->id ? 'selected' : '' ?>><?= $this->escaper->escapeHtml($enquiryType->name) ?></option>
<?php endforeach; ?>
            </select>
          </div>
          <div class="form-group">
            <label for="product_ref">Product ref <span class="text-muted">(optional — e.g. DEP-RESTORE)</span></label>
            <input type="text" class="form-control" id="product_ref" name="product_ref" value="<?= $this->escaper->escapeHtml((string) $article->product_ref) ?>">
          </div>
          <div class="form-group">
            <label class="d-block">Visibility</label>
<?php foreach (\KbArticles::VISIBILITIES as $value => $label): ?>
            <div class="form-check form-check-inline">
              <input class="form-check-input" type="radio" name="visibility" id="visibility_<?= $value ?>" value="<?= $value ?>" <?= ($article->visibility ?: 'internal') === $value ? 'checked' : '' ?>>
              <label class="form-check-label" for="visibility_<?= $value ?>"><?= $label ?></label>
            </div>
<?php endforeach; ?>
            <small class="form-text text-muted">Public articles are servable to the website's FAQ endpoint once published. Internal articles are for agent/staff grounding only.</small>
          </div>
          <div class="form-group">
            <label for="summary">Summary <span class="text-muted">(short, used in lists and match results)</span></label>
            <textarea class="form-control" id="summary" name="summary" rows="2"><?= $this->escaper->escapeHtml((string) $article->summary) ?></textarea>
          </div>
          <div class="form-group">
            <label for="body">Body <span class="text-muted">(Markdown)</span></label>
            <textarea class="form-control" id="body" name="body" rows="14"><?= $this->escaper->escapeHtml((string) $article->body) ?></textarea>
          </div>
          <button type="submit" class="btn btn-primary">Create article (draft)</button>
          <a href="<?= $this->url->get('backend/kb-articles') ?>" class="btn btn-link">Cancel</a>
        </form>
      </div>
    </div>
  </div>
</div>
