<?php $baseUrl = $this->url->get('backend/roles'); ?>
<div class="row mb-2">
  <div class="col-12 d-flex justify-content-between align-items-center">
    <h1>Roles</h1>
    <a href="<?= $this->url->get('backend/roles/new') ?>" class="btn btn-primary">New role</a>
  </div>
</div>

<div class="row mb-2">
  <div class="col-12">
    <?= \App_skeleton\ListView::searchForm($baseUrl, $listState['q'], $preserveQuery, 'Search name…') ?>
  </div>
</div>

<form method="post" action="<?= $this->url->get('backend/roles/bulk') ?>" id="roles-bulk-form">
  <div class="row mb-2">
    <div class="col-12 d-flex align-items-center flex-wrap" style="gap: 0.5rem;">
      <button type="submit" name="bulk_action" value="delete" class="btn btn-sm btn-outline-danger" onclick="return confirm('Delete the selected role(s)? Roles still assigned to a user are skipped. This cannot be undone from here.')">Delete selected</button>
      <span class="text-muted small" id="bulk-selected-count">0 selected</span>
    </div>
  </div>

  <div class="row">
    <div class="col-12">
      <div class="card">
        <div class="card-body p-0">
          <div class="table-responsive">
          <table class="table table-striped mb-0">
            <thead>
              <tr>
                <th style="width: 2.5rem;"><input type="checkbox" id="bulk-select-all" aria-label="Select all roles"></th>
                <th><?= \App_skeleton\ListView::sortLink($baseUrl, 'Name', 'name', $listState['sort'], $listState['dir'], $preserveQuery) ?></th>
                <th><?= \App_skeleton\ListView::sortLink($baseUrl, 'Created', 'created', $listState['sort'], $listState['dir'], $preserveQuery) ?></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
<?php foreach ($roles as $role): ?>
              <tr>
                <td><input type="checkbox" class="bulk-row-checkbox" name="role_ids[]" value="<?= $role->id ?>" aria-label="Select <?= $this->escaper->escapeHtmlAttr($role->name) ?>"></td>
                <td><?= $this->escaper->escapeHtml($role->name) ?></td>
                <td><?= $this->escaper->escapeHtml($role->created_at) ?></td>
                <td class="text-right">
                  <a href="<?= $this->url->get('backend/roles/edit/' . $role->id) ?>" class="btn btn-sm btn-outline-secondary">Edit</a>
                  <button type="submit" formaction="<?= $this->url->get('backend/roles/delete/' . $role->id) ?>" class="btn btn-sm btn-outline-danger" onclick="return confirm('Delete this role?')">Delete</button>
                </td>
              </tr>
<?php endforeach; ?>
<?php if (!count($roles)): ?>
              <tr>
                <td colspan="4" class="text-center text-muted">No roles yet.</td>
              </tr>
<?php endif; ?>
            </tbody>
          </table>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="row mt-2">
    <div class="col-12 d-flex justify-content-between align-items-center">
      <span class="text-muted small"><?= $listState['total'] ?> role<?= $listState['total'] === 1 ? '' : 's' ?></span>
      <?= \App_skeleton\ListView::pagination($baseUrl, $listState['page'], $listState['totalPages'], $preserveQuery) ?>
    </div>
  </div>
</form>

<script src="/assets/js/bulk-select.js"></script>
