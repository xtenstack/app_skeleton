<div class="login-box">
  <div class="login-logo">
    <b>App</b>Skeleton
  </div>
  <div class="card">
    <div class="card-body login-card-body">
      <p class="login-box-msg">Choose a new password</p>

      <?php if (!empty($error)): ?>
      <div class="alert alert-danger"><?= $this->escaper->escapeHtml($error) ?></div>
      <?php endif; ?>

      <form method="post" action="<?= $this->url->get('backend/password/update') ?>">
        <input type="hidden" name="token" value="<?= $this->escaper->escapeHtml($token) ?>">
        <div class="input-group mb-3">
          <input type="password" class="form-control" name="password" placeholder="New password (min. 8 characters)" required minlength="8">
          <div class="input-group-append">
            <div class="input-group-text"><span class="fas fa-lock"></span></div>
          </div>
        </div>
        <div class="row">
          <div class="col-12">
            <button type="submit" class="btn btn-primary btn-block">Update password</button>
          </div>
        </div>
      </form>
    </div>
  </div>
</div>
