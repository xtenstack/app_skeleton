<div class="row mb-2">
  <div class="col-12 d-flex justify-content-between align-items-center">
    <h1>Settings — <?= $this->escaper->escapeHtml($targetUser->email) ?></h1>
    <a href="<?= $this->url->get('backend/users/edit/' . $targetUser->id) ?>" class="btn btn-outline-secondary">Back to user</a>
  </div>
</div>

<div class="row">
  <div class="col-12 col-md-6">
    <div class="card mb-3">
      <div class="card-body p-0">
        <div class="table-responsive">
        <table class="table table-striped mb-0">
          <thead>
            <tr>
              <th>Key</th>
              <th>Value</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
<?php foreach ($settings as $setting): ?>
            <tr>
              <td><?= $this->escaper->escapeHtml($setting->setting_key) ?></td>
              <td><?= $this->escaper->escapeHtml($setting->setting_value) ?></td>
              <td class="text-right">
                <form method="post" action="<?= $this->url->get('backend/users/delete-setting/' . $targetUser->id . '/' . $setting->id) ?>" class="d-inline" onsubmit="return confirm('Remove this setting?')">
                  <button type="submit" class="btn btn-sm btn-outline-danger">Remove</button>
                </form>
              </td>
            </tr>
<?php endforeach; ?>
<?php if (!count($settings)): ?>
            <tr>
              <td colspan="3" class="text-center text-muted">No settings yet.</td>
            </tr>
<?php endif; ?>
          </tbody>
        </table>
        </div>
      </div>
    </div>

    <div class="card">
      <div class="card-body">
        <h5>Add / update a setting</h5>
        <form method="post" action="<?= $this->url->get('backend/users/add-setting/' . $targetUser->id) ?>">
          <div class="form-row">
            <div class="form-group col-6">
              <label for="setting_key">Key</label>
              <input type="text" class="form-control" id="setting_key" name="setting_key" required>
            </div>
            <div class="form-group col-6">
              <label for="setting_value">Value</label>
              <input type="text" class="form-control" id="setting_value" name="setting_value">
            </div>
          </div>
          <button type="submit" class="btn btn-primary">Save</button>
        </form>
      </div>
    </div>
  </div>
</div>
