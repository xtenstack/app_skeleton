<div class="row mb-2">
  <div class="col-12 d-flex justify-content-between align-items-center">
    <h1>Profile — <?= $this->escaper->escapeHtml($targetUser->email) ?></h1>
    <a href="<?= $this->url->get('backend/users/edit/' . $targetUser->id) ?>" class="btn btn-outline-secondary">Back to user</a>
  </div>
</div>

<div class="row">
  <div class="col-12 col-md-6">
    <div class="card">
      <div class="card-body">
        <form method="post" action="<?= $this->url->get('backend/users/profile/' . $targetUser->id) ?>">
          <div class="form-group">
            <label for="phone">Phone</label>
            <input type="text" class="form-control" id="phone" name="phone" value="<?= $this->escaper->escapeHtml($profile->phone) ?>">
          </div>
          <div class="form-group">
            <label for="bio">Bio</label>
            <textarea class="form-control" id="bio" name="bio" rows="4"><?= $this->escaper->escapeHtml($profile->bio) ?></textarea>
          </div>
          <div class="form-row">
            <div class="form-group col-6">
              <label for="timezone">Timezone</label>
              <select class="form-control" id="timezone" name="timezone">
<?php $currentTimezone = $profile->timezone ?: 'UTC'; ?>
<?php foreach ($timezones as $timezone): ?>
                <option value="<?= $this->escaper->escapeHtml($timezone) ?>" <?= $timezone === $currentTimezone ? 'selected' : '' ?>><?= $this->escaper->escapeHtml($timezone) ?></option>
<?php endforeach; ?>
              </select>
            </div>
            <div class="form-group col-6">
              <label for="locale">Locale</label>
              <select class="form-control" id="locale" name="locale">
<?php $currentLocale = $profile->locale ?: 'en-AU'; ?>
<?php foreach ($locales as $code => $name): ?>
                <option value="<?= $this->escaper->escapeHtml($code) ?>" <?= $code === $currentLocale ? 'selected' : '' ?>><?= $this->escaper->escapeHtml($name) ?> (<?= $this->escaper->escapeHtml($code) ?>)</option>
<?php endforeach; ?>
              </select>
            </div>
          </div>
          <div class="form-group form-check">
            <input type="checkbox" class="form-check-input" id="age_verified" name="age_verified" value="1" <?= $profile->age_verified_at ? 'checked' : '' ?>>
            <label class="form-check-label" for="age_verified">
              Age verified (18+, or the relevant jurisdiction's age of majority)
<?php if ($profile->age_verified_at): ?>
              <span class="text-muted">— set <?= $this->escaper->escapeHtml($profile->age_verified_at) ?></span>
<?php endif; ?>
            </label>
            <small class="form-text text-muted">Admin-only. Not visible or settable on the user's own profile page.</small>
          </div>
          <button type="submit" class="btn btn-primary">Save</button>
        </form>
      </div>
    </div>
  </div>
</div>
