<?php
$initial = strtoupper(substr($targetUser->first_name ?: $targetUser->email, 0, 1));
?>
<div class="row mb-2">
  <div class="col-12">
    <h1>My Profile</h1>
  </div>
</div>

<div class="row">
  <div class="col-12 col-md-4">
    <div class="card">
      <div class="card-body text-center">
<?php if ($profile->avatar_path): ?>
        <img src="<?= $this->escaper->escapeHtml($profile->avatar_path) ?>" class="img-circle" style="width:120px;height:120px;object-fit:cover;" alt="Avatar">
<?php else: ?>
        <div class="rounded-circle bg-primary text-white d-flex align-items-center justify-content-center mx-auto" style="width:120px;height:120px;font-size:48px;">
          <?= $this->escaper->escapeHtml($initial) ?>
        </div>
<?php endif; ?>
        <h5 class="mt-3 mb-0"><?= $this->escaper->escapeHtml(trim($targetUser->first_name . ' ' . $targetUser->last_name)) ?: 'Unnamed' ?></h5>
        <p class="text-muted"><?= $this->escaper->escapeHtml($targetUser->email) ?></p>

        <p>
<?php if ($profile->age_verified_at): ?>
          <span class="badge badge-success">Age verified</span>
<?php else: ?>
          <span class="badge badge-secondary">Not age verified</span>
<?php endif; ?>
        </p>
        <p class="text-muted small">Age verification is set by an administrator, not here.</p>

        <form method="post" action="<?= $this->url->get('backend/account/upload-avatar') ?>" enctype="multipart/form-data">
          <div class="form-group">
            <input type="file" class="form-control-file" name="avatar" accept="image/jpeg,image/png,image/webp" required>
          </div>
          <button type="submit" class="btn btn-outline-primary btn-block btn-sm">Upload avatar</button>
        </form>
      </div>
    </div>

    <div class="card">
      <div class="card-header">
        <strong>API Keys</strong>
      </div>
      <div class="card-body p-0">
        <table class="table table-sm mb-0">
          <tbody>
<?php foreach ($apiKeys as $apiKey): ?>
            <tr>
              <td><?= $this->escaper->escapeHtml($apiKey->name) ?></td>
              <td class="text-right"><code><?= $this->escaper->escapeHtml($apiKey->token_prefix) ?>…</code></td>
            </tr>
<?php endforeach; ?>
<?php if (!count($apiKeys)): ?>
            <tr>
              <td class="text-center text-muted">No active keys.</td>
            </tr>
<?php endif; ?>
          </tbody>
        </table>
      </div>
      <div class="card-footer text-right">
        <a href="<?= $this->url->get('backend/api-keys') ?>" class="btn btn-sm btn-outline-secondary">Generate / manage keys</a>
      </div>
    </div>
  </div>

  <div class="col-12 col-md-8">
    <div class="card">
      <div class="card-body">
        <form method="post" action="<?= $this->url->get('backend/account/update') ?>">
          <div class="form-row">
            <div class="form-group col-6">
              <label for="first_name">First name</label>
              <input type="text" class="form-control" id="first_name" name="first_name" value="<?= $this->escaper->escapeHtml((string) $targetUser->first_name) ?>">
            </div>
            <div class="form-group col-6">
              <label for="last_name">Last name</label>
              <input type="text" class="form-control" id="last_name" name="last_name" value="<?= $this->escaper->escapeHtml((string) $targetUser->last_name) ?>">
            </div>
          </div>
          <div class="form-group">
            <label>Email</label>
            <input type="email" class="form-control" value="<?= $this->escaper->escapeHtml($targetUser->email) ?>" disabled>
          </div>
          <div class="form-group">
            <label for="phone">Phone</label>
            <input type="text" class="form-control" id="phone" name="phone" value="<?= $this->escaper->escapeHtml((string) $profile->phone) ?>">
          </div>
          <div class="form-group">
            <label for="bio">Bio</label>
            <textarea class="form-control" id="bio" name="bio" rows="3"><?= $this->escaper->escapeHtml((string) $profile->bio) ?></textarea>
          </div>
          <div class="form-row">
            <div class="form-group col-6">
              <label for="timezone">Timezone</label>
              <input type="text" class="form-control" id="timezone" name="timezone" value="<?= $this->escaper->escapeHtml($profile->timezone ?: 'UTC') ?>">
            </div>
            <div class="form-group col-6">
              <label for="locale">Locale</label>
              <input type="text" class="form-control" id="locale" name="locale" value="<?= $this->escaper->escapeHtml($profile->locale ?: 'en-AU') ?>">
            </div>
          </div>
          <button type="submit" class="btn btn-primary">Save</button>
        </form>
      </div>
    </div>
  </div>
</div>
