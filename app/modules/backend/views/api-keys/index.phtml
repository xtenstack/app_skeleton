<?php $baseUrl = $this->url->get('backend/api-keys'); ?>
<div class="row mb-2">
  <div class="col-12">
    <h1>API Keys (Internal)</h1>
    <p class="text-muted mb-0">Credentials other systems use to call <em>our</em> API. For connections <em>we</em> make to third-party APIs, see External Connections.</p>
  </div>
</div>

<?php if (!empty($newToken)): ?>
<div class="row">
  <div class="col-12">
    <div class="alert alert-warning">
      <strong>Copy this key now — it won't be shown again:</strong>
      <pre class="mb-0 mt-2"><?= $this->escaper->escapeHtml($newToken) ?></pre>
    </div>
  </div>
</div>
<?php endif; ?>

<div class="row mb-2">
  <div class="col-12">
    <div class="btn-group" role="group">
      <a href="<?= $baseUrl ?>" class="btn btn-sm btn-outline-secondary <?= $currentStatus === '' ? 'active' : '' ?>">All</a>
      <a href="<?= $baseUrl ?>?status=active" class="btn btn-sm btn-outline-secondary <?= $currentStatus === 'active' ? 'active' : '' ?>">Active</a>
      <a href="<?= $baseUrl ?>?status=revoked" class="btn btn-sm btn-outline-secondary <?= $currentStatus === 'revoked' ? 'active' : '' ?>">Revoked</a>
    </div>
  </div>
</div>

<div class="row mb-2">
  <div class="col-12">
    <?= \App_skeleton\ListView::searchForm($baseUrl, $listState['q'], $preserveQuery, 'Search name…') ?>
  </div>
</div>

<form method="post" action="<?= $this->url->get('backend/api-keys/bulk') ?>" id="api-keys-bulk-form">
  <div class="row mb-2">
    <div class="col-12 d-flex align-items-center flex-wrap" style="gap: 0.5rem;">
      <button type="submit" name="bulk_action" value="revoke" class="btn btn-sm btn-outline-danger" onclick="return confirm('Revoke the selected key(s)?')">Revoke selected</button>
      <span class="text-muted small" id="bulk-selected-count">0 selected</span>
    </div>
  </div>

  <div class="row">
    <div class="col-12">
      <div class="card mb-3">
        <div class="card-body p-0">
          <div class="table-responsive">
          <table class="table table-striped mb-0">
            <thead>
              <tr>
                <th style="width: 2.5rem;"><input type="checkbox" id="bulk-select-all" aria-label="Select all API keys"></th>
                <th><?= \App_skeleton\ListView::sortLink($baseUrl, 'Name', 'name', $listState['sort'], $listState['dir'], $preserveQuery) ?></th>
                <th>Key</th>
                <th><?= \App_skeleton\ListView::sortLink($baseUrl, 'Last used', 'last_used', $listState['sort'], $listState['dir'], $preserveQuery) ?></th>
                <th>Status</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
<?php foreach ($apiKeys as $apiKey): ?>
              <tr>
                <td><input type="checkbox" class="bulk-row-checkbox" name="api_key_ids[]" value="<?= $apiKey->id ?>" aria-label="Select <?= $this->escaper->escapeHtmlAttr($apiKey->name) ?>"></td>
                <td><?= $this->escaper->escapeHtml($apiKey->name) ?></td>
                <td><code><?= $this->escaper->escapeHtml($apiKey->token_prefix) ?>…</code></td>
                <td><?= $this->escaper->escapeHtml($apiKey->last_used_at ?: 'Never') ?></td>
                <td>
<?php if ($apiKey->revoked_at): ?>
                  <span class="badge badge-secondary">Revoked</span>
<?php else: ?>
                  <span class="badge badge-success">Active</span>
<?php endif; ?>
                </td>
                <td class="text-right">
<?php if (!$apiKey->revoked_at): ?>
                  <button type="submit" formaction="<?= $this->url->get('backend/api-keys/revoke/' . $apiKey->id) ?>" class="btn btn-sm btn-outline-danger" onclick="return confirm('Revoke this key?')">Revoke</button>
<?php endif; ?>
                </td>
              </tr>
<?php endforeach; ?>
<?php if (!count($apiKeys)): ?>
              <tr>
                <td colspan="6" class="text-center text-muted">No API keys yet.</td>
              </tr>
<?php endif; ?>
            </tbody>
          </table>
          </div>
        </div>
      </div>
      <div class="row mb-3">
        <div class="col-12 d-flex justify-content-between align-items-center">
          <span class="text-muted small"><?= $listState['total'] ?> API key<?= $listState['total'] === 1 ? '' : 's' ?></span>
          <?= \App_skeleton\ListView::pagination($baseUrl, $listState['page'], $listState['totalPages'], $preserveQuery) ?>
        </div>
      </div>
    </div>
  </div>
</form>

<div class="row">
  <div class="col-12">
    <div class="card">
      <div class="card-body">
        <h5>Create a new key</h5>
        <form method="post" action="<?= $this->url->get('backend/api-keys/create') ?>" class="form-inline">
          <input type="text" class="form-control mr-2" name="name" placeholder="Key name (e.g. My integration)" required>
          <button type="submit" class="btn btn-primary">Generate</button>
        </form>
      </div>
    </div>
  </div>
</div>

<script src="/assets/js/bulk-select.js"></script>
