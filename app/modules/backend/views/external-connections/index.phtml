<?php $baseUrl = $this->url->get('backend/external-connections'); ?>
<div class="row mb-2">
  <div class="col-12 d-flex justify-content-between align-items-center">
    <h1>External Connections</h1>
    <a href="<?= $this->url->get('backend/external-connections/new') ?>" class="btn btn-primary">New connection</a>
  </div>
</div>

<div class="row mb-2">
  <div class="col-12">
    <?= \App_skeleton\ListView::searchForm($baseUrl, $listState['q'], $preserveQuery, 'Search name/base URL…') ?>
  </div>
</div>

<form method="post" action="<?= $this->url->get('backend/external-connections/bulk') ?>" id="external-connections-bulk-form">
  <div class="row mb-2">
    <div class="col-12 d-flex align-items-center flex-wrap" style="gap: 0.5rem;">
      <div class="dropdown">
        <button class="btn btn-sm btn-outline-secondary dropdown-toggle" type="button" id="bulk-dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
          With selected
        </button>
        <div class="dropdown-menu p-3 shadow" style="min-width: 300px;" aria-labelledby="bulk-dropdown-toggle">
          <div class="form-group mb-3">
            <label class="small mb-1">Set status</label>
            <select class="form-control form-control-sm" name="is_active">
              <option value="">— No change —</option>
              <option value="1">Active</option>
              <option value="0">Inactive</option>
            </select>
          </div>
          <button type="submit" name="bulk_action" value="update" class="btn btn-sm btn-primary btn-block">Apply to selected</button>
          <button type="submit" name="bulk_action" value="delete" class="btn btn-sm btn-outline-danger btn-block" onclick="return confirm('Delete the selected connection(s)? This cannot be undone from here.')">Delete selected</button>
        </div>
      </div>
      <span class="text-muted small" id="bulk-selected-count">0 selected</span>
    </div>
  </div>

  <div class="row">
    <div class="col-12">
      <div class="card">
        <div class="card-body p-0">
          <div class="table-responsive">
          <table class="table table-striped mb-0">
            <thead>
              <tr>
                <th style="width: 2.5rem;"><input type="checkbox" id="bulk-select-all" aria-label="Select all external connections"></th>
                <th><?= \App_skeleton\ListView::sortLink($baseUrl, 'Name', 'name', $listState['sort'], $listState['dir'], $preserveQuery) ?></th>
                <th>Base URL</th>
                <th><?= \App_skeleton\ListView::sortLink($baseUrl, 'Auth type', 'auth_type', $listState['sort'], $listState['dir'], $preserveQuery) ?></th>
                <th>Status</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
<?php foreach ($connections as $connection): ?>
              <tr>
                <td><input type="checkbox" class="bulk-row-checkbox" name="external_connection_ids[]" value="<?= $connection->id ?>" aria-label="Select <?= $this->escaper->escapeHtmlAttr($connection->name) ?>"></td>
                <td><?= $this->escaper->escapeHtml($connection->name) ?></td>
                <td><?= $this->escaper->escapeHtml($connection->base_url ?: '—') ?></td>
                <td><code><?= $this->escaper->escapeHtml($connection->auth_type) ?></code></td>
                <td>
<?php if ($connection->is_active): ?>
                  <span class="badge badge-success">Active</span>
<?php else: ?>
                  <span class="badge badge-secondary">Inactive</span>
<?php endif; ?>
                </td>
                <td class="text-right">
                  <a href="<?= $this->url->get('backend/external-connections/edit/' . $connection->id) ?>" class="btn btn-sm btn-outline-secondary">Edit</a>
                  <button type="submit" formaction="<?= $this->url->get('backend/external-connections/delete/' . $connection->id) ?>" class="btn btn-sm btn-outline-danger" onclick="return confirm('Delete this connection?')">Delete</button>
                </td>
              </tr>
<?php endforeach; ?>
<?php if (!count($connections)): ?>
              <tr>
                <td colspan="6" class="text-center text-muted">No external connections yet.</td>
              </tr>
<?php endif; ?>
            </tbody>
          </table>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="row mt-2">
    <div class="col-12 d-flex justify-content-between align-items-center">
      <span class="text-muted small"><?= $listState['total'] ?> connection<?= $listState['total'] === 1 ? '' : 's' ?></span>
      <?= \App_skeleton\ListView::pagination($baseUrl, $listState['page'], $listState['totalPages'], $preserveQuery) ?>
    </div>
  </div>
</form>

<script src="/assets/js/bulk-select.js"></script>
