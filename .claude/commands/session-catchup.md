---
description: Pick up where the last session left off — branch status, open PRs, and the last Handover's "First thing, next session" section
---

Get me oriented for this session, fast. Do the following and report back
concisely (a few sentences, not a wall of output):

1. Run `git status` and `git log --oneline -10` in this repo to see
   current branch state and recent commits.
2. Run `gh pr list --repo xtenstack/app_skeleton` to see any open PRs.
3. Find the most recent `Handover-YYYY-MM-DD.md` in
   `stack.xten.au/claude/sessions/` (private, not in this repo) and
   read its `## First thing, next session` and
   `## Needed from the user to unblock other things` sections.
4. Summarize: what's the repo's current state, what's the next agenda
   item per the last handover, and is anything blocked pending the
   user's own action.

Don't start acting on any of it yet — this is orientation only, report
back and wait for direction.
