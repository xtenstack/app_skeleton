{
  "name": "app_skeleton-e2e",
  "private": true,
  "description": "Playwright E2E tests only — the app itself is 100% PHP (Phalcon), no JS build step. See docs/CODING-STANDARDS.md's Testing section and tests/e2e/README.md.",
  "scripts": {
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
